import React, { Component, PropTypes } from 'react';
import { Header, IntroCover, Footer } from '../components';

const App = () => {
	return (
		<div>
			<Header/>
			<IntroCover/>
			<Footer/>
		</div>
	);
};

export default App;
